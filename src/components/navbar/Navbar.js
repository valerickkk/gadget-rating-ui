import "./style.css"

const Navbar = () => {

    return ( 
        <nav className="nav">
            <div className="container">
                <div className="nav-row">
                    <a className="logo"><strong>GadRat</strong></a>

                    <ul className="nav-list">
                    </ul>
                </div>
            </div>
        </nav>
    );
}
 
export default Navbar;